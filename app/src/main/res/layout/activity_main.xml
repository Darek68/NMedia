

<androidx.constraintlayout.widget.ConstraintLayout
xmlns:android="http://schemas.android.com/apk/res/android"
xmlns:app="http://schemas.android.com/apk/res-auto"
xmlns:tools="http://schemas.android.com/tools"
android:layout_width="match_parent"
android:layout_height="match_parent"
tools:context=".activity.MainActivity">

<androidx.recyclerview.widget.RecyclerView
    android:id="@+id/list"
    android:layout_width="match_parent"
    android:layout_height="0dp"
    app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
    app:layout_constraintBottom_toTopOf="@id/toolsTop"
    app:layout_constraintTop_toTopOf="parent"
    tools:listitem="@layout/card_post" />

<androidx.constraintlayout.widget.Barrier
    android:id="@+id/toolsTop"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    app:barrierDirection="top"
     />
    <!--
    app:constraint_referenced_ids="content,save"
    <EditText
        android:id="@+id/content"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="@string/post_text"
        android:inputType="textMultiLine"
        android:background="@android:color/transparent"
        android:padding="@dimen/common_spacing"
        android:maxLines="6"
        android:importantForAutofill="no"
        app:layout_constraintTop_toTopOf="@id/toolsTop"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/save"
        app:layout_constraintStart_toStartOf="parent"
        />

        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/constrPen"
            android:layout_width="match_parent"
            android:layout_height="@dimen/posts_avatar_size"
            android:background="@color/white"
            app:layout_constraintBottom_toTopOf="@id/content"
            tools:context=".activity.MainActivity">
            <androidx.constraintlayout.widget.Group
                android:id="@+id/group"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:visibility="visible"
                app:layout_constraintBottom_toBottomOf="@id/constrPen"
                app:constraint_referenced_ids="cancel,createPen,textTitl,textCont,constrPen" />
        <ImageView
            android:id="@+id/createPen"
            android:layout_width="@dimen/pic_scale"
            android:layout_height="@dimen/pic_scale"
            android:scaleType="fitXY"
            android:layout_marginTop="@dimen/common_spacing"
            android:background="@android:color/transparent"
            android:contentDescription="@string/description_post_like"
            app:layout_constraintStart_toStartOf="@id/constrPen"
            app:layout_constraintBottom_toBottomOf="@id/constrPen"
            app:srcCompat="@drawable/ic_baseline_create_24"
            />
        <TextView
            android:id="@+id/textTitl"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/common_spacing"
            android:layout_marginStart="@dimen/common_spacing"
            android:layout_marginEnd="@dimen/common_spacing"
            android:ellipsize="end"
            android:singleLine="true"
            android:textStyle="bold"
            android:textColor="@color/teal_700"
            android:text="@string/edit_message"
            app:layout_constraintBottom_toTopOf="@id/textCont"
            app:layout_constraintStart_toEndOf="@id/createPen"
            app:layout_constraintEnd_toStartOf="@id/cancel"
            app:layout_constraintVertical_chainStyle="packed"
            tools:text="@string/edit_message" />
        <TextView
            android:id="@+id/textCont"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/common_spacing"
            android:layout_marginStart="@dimen/common_spacing"
            android:layout_marginEnd="@dimen/common_spacing"
            android:ellipsize="end"
            android:singleLine="true"
            android:maxLines="1"
            app:layout_constraintBottom_toBottomOf="@id/constrPen"
            app:layout_constraintStart_toEndOf="@id/createPen"
            app:layout_constraintEnd_toStartOf="@id/cancel"
            app:layout_constraintVertical_chainStyle="packed"
            tools:text="@string/first_str" />
        <ImageButton
            android:id="@+id/cancel"
            android:layout_width="@dimen/pic_scale"
            android:layout_height="@dimen/pic_scale"
            android:scaleType="fitXY"
            android:layout_marginTop="@dimen/common_spacing"
            android:background="@android:color/transparent"
            android:contentDescription="@string/description_post_like"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="@id/constrPen"
            app:srcCompat="@drawable/ic_baseline_cancel_24"
            />
            </androidx.constraintlayout.widget.ConstraintLayout>
        <ImageButton
        android:id="@+id/save"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/common_spacing"
        android:background="@android:color/transparent"
        android:contentDescription="@string/description_post_like"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/content"
        app:layout_constraintBottom_toBottomOf="parent"
        app:srcCompat="@drawable/ic_save_24"
        />
       -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/fab_margin"
        android:clickable="true"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:srcCompat="@drawable/ic_add_24dp"
        android:contentDescription="@string/add_post"/>

</androidx.constraintlayout.widget.ConstraintLayout>